# 🚨 СРОЧНО: Отозвать токен бота

## Что случилось?

Токен бота попал в Git и GitHub обнаружил его. Токен скомпрометирован и должен быть отозван.

## Что делать (3 минуты)

### Шаг 1: Отозвать старый токен

1. Открой Telegram
2. Найди **@BotFather**
3. Отправь команду: `/mybots`
4. Выбери: **petappkent_bot**
5. Нажми: **API Token**
6. Нажми: **Revoke current token**
7. Подтверди

### Шаг 2: Получить новый токен

После отзыва BotFather сразу покажет новый токен.

Скопируй его!

### Шаг 3: Обновить bot/.env

Открой файл `bot/.env` и замени строку:

```env
BOT_TOKEN=старый_токен
```

На:

```env
BOT_TOKEN=новый_токен_от_botfather
```

### Шаг 4: Бот перезапустится

Он автоматически подхватит новый токен (tsx watch).

### Шаг 5: Протестируй

1. Telegram → @petappkent_bot
2. Отправь: `/start`
3. Должен ответить!

## Почему это важно?

❌ Старый токен в Git = любой может управлять твоим ботом
✅ Новый токен только в `.env` = безопасно

## После отзыва

Я уже удалил токен из документации в Git. После того как ты отзовёшь токен и получишь новый - всё будет в порядке.

---

**НАЧНИ С ШАГА 1** - отзови токен прямо сейчас! 🚨
