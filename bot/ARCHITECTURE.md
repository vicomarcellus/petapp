# 🏗️ Архитектура Telegram бота

## Общая схема

```
┌─────────────────────────────────────────────────────────────┐
│                         Пользователь                         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      Telegram Client                         │
│                    (мобильное приложение)                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      Telegram Bot API                        │
│                   (api.telegram.org)                         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                         Наш бот                              │
│                    (Node.js + Grammy)                        │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Handlers (обработчики команд)                     │    │
│  │  • /start, /help                                   │    │
│  │  • /link (привязка аккаунта)                       │    │
│  │  • /today (просмотр записей)                       │    │
│  │  • Добавление записей                              │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Session (состояние пользователя)                  │    │
│  │  • Текущее действие                                │    │
│  │  • Временные данные                                │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      Supabase Client                         │
│                   (@supabase/supabase-js)                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                         Supabase                             │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  PostgreSQL Database                               │    │
│  │  • telegram_users (связь аккаунтов)               │    │
│  │  • telegram_link_codes (коды привязки)            │    │
│  │  • pets (питомцы)                                  │    │
│  │  • state_entries (состояния)                      │    │
│  │  • symptom_entries (симптомы)                     │    │
│  │  • medication_entries (лекарства)                 │    │
│  │  • feeding_entries (питание)                      │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Row Level Security (RLS)                          │    │
│  │  • Пользователи видят только свои данные          │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

## Поток данных

### 1. Привязка аккаунта

```
Веб-приложение                    Бот                    База данных
      │                            │                           │
      │ Генерирует код             │                           │
      ├───────────────────────────────────────────────────────>│
      │                            │                           │
      │                            │ /link КОД                 │
      │                            │<──────────                │
      │                            │                           │
      │                            │ Проверяет код             │
      │                            ├──────────────────────────>│
      │                            │                           │
      │                            │ Создаёт связь             │
      │                            ├──────────────────────────>│
      │                            │                           │
      │                            │ ✅ Привязано              │
      │                            │<──────────                │
```

### 2. Добавление записи

```
Пользователь                      Бот                    База данных
      │                            │                           │
      │ ➕ Добавить → 😊 Состояние │                           │
      ├───────────────────────────>│                           │
      │                            │                           │
      │                            │ Показывает клавиатуру     │
      │<───────────────────────────┤                           │
      │                            │                           │
      │ Выбирает оценку (5)        │                           │
      ├───────────────────────────>│                           │
      │                            │                           │
      │                            │ Получает активного питомца│
      │                            ├──────────────────────────>│
      │                            │                           │
      │                            │ Сохраняет запись          │
      │                            ├──────────────────────────>│
      │                            │                           │
      │ ✅ Состояние добавлено     │                           │
      │<───────────────────────────┤                           │
```

### 3. Просмотр записей

```
Пользователь                      Бот                    База данных
      │                            │                           │
      │ /today                     │                           │
      ├───────────────────────────>│                           │
      │                            │                           │
      │                            │ Получает user_id          │
      │                            ├──────────────────────────>│
      │                            │                           │
      │                            │ Получает активного питомца│
      │                            ├──────────────────────────>│
      │                            │                           │
      │                            │ Загружает записи за сегодня│
      │                            ├──────────────────────────>│
      │                            │                           │
      │ 📊 Записи за сегодня       │                           │
      │<───────────────────────────┤                           │
```

## Структура кода

```
bot/
├── src/
│   ├── index.ts              # Главный файл, инициализация бота
│   │   └── Bot instance
│   │       ├── Session middleware
│   │       ├── Command handlers
│   │       ├── Callback handlers
│   │       └── Message handlers
│   │
│   ├── config.ts             # Конфигурация (env переменные)
│   │
│   ├── types.ts              # TypeScript типы
│   │   ├── Pet
│   │   ├── StateEntry
│   │   ├── SymptomEntry
│   │   ├── MedicationEntry
│   │   ├── FeedingEntry
│   │   └── UserContext
│   │
│   ├── supabase.ts           # Supabase клиент
│   │
│   ├── db.ts                 # Функции работы с БД
│   │   ├── linkTelegramUser()
│   │   ├── getSupabaseUserId()
│   │   ├── getActivePet()
│   │   ├── addStateEntry()
│   │   ├── addSymptomEntry()
│   │   ├── addMedicationEntry()
│   │   ├── addFeedingEntry()
│   │   └── getTodayEntries()
│   │
│   ├── keyboards.ts          # Клавиатуры Telegram
│   │   ├── mainMenuKeyboard()
│   │   ├── addMenuKeyboard()
│   │   ├── stateScoreKeyboard()
│   │   ├── medicationUnitsKeyboard()
│   │   └── feedingUnitsKeyboard()
│   │
│   └── handlers/             # Обработчики команд
│       ├── start.ts          # /start, /help
│       ├── link.ts           # /link (привязка)
│       ├── today.ts          # /today (просмотр)
│       └── add.ts            # Добавление записей
│
├── package.json
├── tsconfig.json
└── .env
```

## Безопасность

### RLS Политики

```sql
-- Пользователи видят только свои данные
CREATE POLICY "Users can view their own telegram link"
  ON telegram_users FOR SELECT
  USING (auth.uid() = supabase_user_id);

-- Пользователи могут создавать только свои записи
CREATE POLICY "Users can insert their own attachments"
  ON attachments FOR INSERT
  WITH CHECK (auth.uid() = user_id);
```

### Коды привязки

- ⏱️ Действуют 10 минут
- 🔒 Одноразовые (нельзя использовать дважды)
- 🗑️ Автоматически удаляются через 24 часа

### Сессии

- 💾 Хранятся в памяти бота
- 🔄 Сбрасываются при перезапуске
- 🚫 Не содержат чувствительных данных

## Масштабирование

### Текущая архитектура (Long Polling)

```
Bot <──────> Telegram API
 │
 └──────> Supabase
```

**Плюсы:**
- ✅ Простая настройка
- ✅ Работает везде (даже за NAT)
- ✅ Не нужен публичный URL

**Минусы:**
- ❌ Постоянное соединение
- ❌ Нельзя горизонтально масштабировать

### Продакшен архитектура (Webhooks)

```
Telegram API ──> Load Balancer ──> Bot Instance 1
                                 ├─> Bot Instance 2
                                 └─> Bot Instance 3
                                      │
                                      └──> Supabase
```

**Плюсы:**
- ✅ Горизонтальное масштабирование
- ✅ Меньше нагрузки
- ✅ Быстрее отклик

**Минусы:**
- ❌ Нужен публичный HTTPS URL
- ❌ Сложнее настройка

## Мониторинг

### Логирование

```typescript
// Все ошибки логируются
bot.catch((err) => {
  console.error('Bot error:', err);
});

// Важные события
console.log('User linked:', telegramId);
console.log('Entry added:', entryId);
```

### Метрики (TODO)

- Количество активных пользователей
- Количество записей в день
- Время отклика
- Ошибки

## Roadmap

### Фаза 1 (Текущая)
- ✅ Базовые команды
- ✅ Привязка аккаунта
- ✅ Добавление записей
- ✅ Просмотр записей

### Фаза 2
- [ ] Напоминания
- [ ] Загрузка фото
- [ ] Выбор питомца

### Фаза 3
- [ ] Статистика
- [ ] Экспорт данных
- [ ] Голосовые сообщения

### Фаза 4
- [ ] Webhooks
- [ ] Горизонтальное масштабирование
- [ ] Мониторинг и аналитика
