# 🚀 Деплой Telegram бота

## Вариант 1: Railway (Рекомендуется) ⭐

Railway идеально подходит для Telegram ботов - поддерживает long-running процессы.

### Шаг 1: Создать аккаунт

1. Открой https://railway.app
2. Нажми **"Start a New Project"**
3. Войди через GitHub

### Шаг 2: Создать проект

1. Нажми **"New Project"**
2. Выбери **"Deploy from GitHub repo"**
3. Выбери репозиторий `vicomarcellus/petapp`
4. Railway автоматически обнаружит проект

### Шаг 3: Настроить Root Directory

1. В настройках проекта найди **"Root Directory"**
2. Установи: `bot`
3. Это скажет Railway что код бота в папке `bot`

### Шаг 4: Добавить переменные окружения

В разделе **Variables** добавь:

```
BOT_TOKEN=твой_новый_токен_от_botfather
SUPABASE_URL=https://jweegvbywvixwzcliyzr.supabase.co
SUPABASE_SERVICE_KEY=твой_service_role_ключ
```

### Шаг 5: Деплой

1. Нажми **"Deploy"**
2. Railway автоматически:
   - Установит зависимости (`npm install`)
   - Соберёт проект (`npm run build`)
   - Запустит бота (`npm start`)

### Шаг 6: Проверить логи

1. Открой вкладку **"Deployments"**
2. Кликни на последний деплой
3. Открой **"Logs"**
4. Должно быть: `✅ Bot @petappkent_bot is running!`

### Готово! 🎉

Бот теперь работает 24/7 на серверах Railway.

---

## Вариант 2: Render

Render тоже хороший вариант, бесплатный план.

### Шаг 1: Создать аккаунт

1. Открой https://render.com
2. Войди через GitHub

### Шаг 2: Создать Web Service

1. Нажми **"New +"** → **"Web Service"**
2. Подключи репозиторий `vicomarcellus/petapp`
3. Настройки:
   - **Name**: `petapp-bot`
   - **Root Directory**: `bot`
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`

### Шаг 3: Добавить переменные

В разделе **Environment** добавь:
- `BOT_TOKEN`
- `SUPABASE_URL`
- `SUPABASE_SERVICE_KEY`

### Шаг 4: Деплой

Нажми **"Create Web Service"** - Render задеплоит автоматически.

---

## Вариант 3: VPS (Для продвинутых)

Если у тебя есть свой сервер (DigitalOcean, AWS, etc):

```bash
# На сервере:
git clone https://github.com/vicomarcellus/petapp.git
cd petapp/bot
npm install
npm run build

# Создать .env файл
nano .env
# Вставить переменные окружения

# Запустить с PM2
npm install -g pm2
pm2 start dist/index.js --name petapp-bot
pm2 save
pm2 startup
```

---

## Что выбрать?

| Платформа | Плюсы | Минусы |
|-----------|-------|--------|
| **Railway** ⭐ | Простой, быстрый, бесплатный план | Ограничения на бесплатном плане |
| **Render** | Бесплатный план, простой | Может засыпать на бесплатном плане |
| **VPS** | Полный контроль | Нужно настраивать самому |

**Рекомендация:** Начни с **Railway** - это самый простой вариант! 🚀

---

## После деплоя

1. Проверь логи - должно быть `✅ Bot @petappkent_bot is running!`
2. Протестируй бота в Telegram
3. Можешь выключить локальный бот (Ctrl+C)
4. Бот теперь работает 24/7! 🎉

## Автоматические обновления

После деплоя каждый `git push` будет автоматически обновлять бота на сервере!

```bash
# Внёс изменения в код
git add .
git commit -m "Update bot"
git push

# Railway/Render автоматически задеплоят новую версию!
```
