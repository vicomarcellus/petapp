# 🔧 ИСПРАВЛЕНИЕ БОТА - СДЕЛАЙ ЭТО СЕЙЧАС

## Проблема
Бот не может записывать данные в БД из-за RLS политик.

## Решение (2 минуты)

### Шаг 1: Получи Service Role Key

1. Открой Supabase Dashboard: https://supabase.com/dashboard
2. Выбери свой проект
3. Перейди в **Settings** (⚙️) → **API**
4. Найди секцию **Project API keys**
5. Скопируй **service_role** ключ (длинная строка, начинается с `eyJ...`)

⚠️ **НЕ путай с anon key!** Нужен именно **service_role**!

### Шаг 2: Обнови bot/.env

Открой файл `bot/.env` и замени строку:

```env
SUPABASE_SERVICE_KEY=ПОЛУЧИ_КЛЮЧ_В_SUPABASE_DASHBOARD
```

На:

```env
SUPABASE_SERVICE_KEY=твой_скопированный_ключ_здесь
```

Должно получиться примерно так:

```env
# Telegram Bot Token
BOT_TOKEN=ТВОЙ_ТОКЕН_ОТ_BOTFATHER

# Supabase
SUPABASE_URL=https://твой-проект.supabase.co
SUPABASE_SERVICE_KEY=ТВОЙ_SERVICE_ROLE_КЛЮЧ_ЗДЕСЬ

# Webhook
WEBHOOK_URL=
PORT=3000
```

### Шаг 3: Перезапусти бота

Бот автоматически перезапустится (tsx watch), но если нет:

```bash
# Останови бота (Ctrl+C)
# Запусти снова:
cd bot
npm run dev
```

### Шаг 4: Протестируй

1. Открой https://petapp-omega.vercel.app
2. Настройки → Telegram Bot
3. Метод 2 → "Сгенерировать код"
4. Скопируй код
5. Telegram → @petappkent_bot
6. Отправь: `/link КОД`
7. Должно быть: ✅ Аккаунт успешно привязан!

## Почему это безопасно?

✅ Service key используется только на сервере (не в браузере)
✅ Файл `.env` в `.gitignore` (не попадёт в Git)
✅ Бот работает на твоём компьютере/сервере

## Если не работает

Проверь логи бота в терминале. Должно быть:

```
✅ Bot @petappkent_bot is running!
```

Если видишь ошибку - скопируй её и покажи мне.

---

**НАЧНИ С ШАГА 1** - получи service_role ключ! 🔑
