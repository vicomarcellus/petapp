# Шаги миграции на Supabase

## ✅ Что уже сделано:

1. Установлен пакет `@supabase/supabase-js`
2. Создана конфигурация Supabase (`src/lib/supabase.ts`)
3. Создана SQL схема (`supabase-schema.sql`)
4. Обновлены типы данных для работы с Supabase
5. Переписаны компоненты:
   - ✅ `Auth.tsx` - аутентификация через Supabase
   - ✅ `PetManager.tsx` - управление питомцами
   - ✅ `Header.tsx` - навигация и выбор питомца
   - ✅ `Settings.tsx` - настройки профиля и теги
   - ✅ `usePetInit.ts` - инициализация питомца
6. Настроены ключи Supabase в `.env`
7. Создана инструкция по настройке (`SUPABASE_SETUP.md`)

## 🔴 Что нужно сделать СЕЙЧАС:

### ✅ ВЫПОЛНЕНО! Ключи настроены.

Теперь нужно:
1. **Выполнить SQL схему** в Supabase SQL Editor (файл `supabase-schema.sql`)
2. **Перезапустить dev сервер**: `npm run dev`
3. **Протестировать** регистрацию и вход

## 📋 Что нужно будет сделать ПОСЛЕ:

После выполнения SQL схемы нужно переписать остальные компоненты:

- [ ] `Calendar.tsx` - календарь с записями
- [ ] `EntryView.tsx` - просмотр и редактирование записей  
- [ ] `Checklist.tsx` - чеклист задач
- [ ] `ActivityLog.tsx` - лог активности
- [ ] `Analytics.tsx` - аналитика
- [ ] `MedicationManager.tsx` - управление лекарствами
- [ ] `QuickChat.tsx` - быстрый чат с AI
- [ ] Все сервисы в `src/services/`
- [ ] Хуки `useAutoBackup.ts` и `useTaskNotifications.tsx`
- [ ] Удалить `src/db.ts` (IndexedDB больше не нужен)

## 🚀 После миграции:

- Вы сможете входить с любого устройства
- Данные будут синхронизироваться автоматически
- Можно будет настроить Vercel для деплоя

---

**ВАЖНО:** Выполните SQL схему из файла `supabase-schema.sql` в Supabase SQL Editor, затем перезапустите сервер!
