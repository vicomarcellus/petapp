#!/bin/bash

# Переход в директорию скрипта
cd "$(dirname "$0")"

# Цвета для вывода
GREEN='\033[0;32m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

echo -e "${BLUE}🚀 Запуск Cat Health Tracker...${NC}"

# Проверка наличия node_modules
if [ ! -d "node_modules" ]; then
    echo -e "${BLUE}📦 Установка зависимостей...${NC}"
    npm install
fi

# Проверка наличия .env файла
if [ ! -f ".env" ]; then
    echo -e "${BLUE}⚠️  Файл .env не найден. Создайте его на основе .env.example${NC}"
fi

echo -e "${GREEN}✓ Запуск dev сервера...${NC}"
echo -e "${GREEN}✓ Страница откроется автоматически${NC}"
echo ""

# Запуск dev сервера (Vite автоматически откроет браузер)
npm run dev -- --open
