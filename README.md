# 🐾 Pet Health Tracker

Веб-приложение для отслеживания здоровья питомцев.

## Возможности

- 📊 Отслеживание состояния питомца (оценка 1-5)
- 🤒 Запись симптомов
- 💊 Учёт лекарств и дозировок
- 🍽️ Контроль питания
- 📅 Календарь записей
- 📈 Аналитика и графики
- 🔔 Планировщик событий
- 📎 Прикрепление файлов к записям
- 🏥 Управление диагнозами
- 🤖 AI-помощник для анализа

## Технологии

- **Frontend**: React + TypeScript + Vite
- **Styling**: Tailwind CSS
- **Database**: Supabase (PostgreSQL)
- **Storage**: Supabase Storage
- **State**: Zustand
- **Charts**: Recharts
- **Deployment**: Vercel

## Установка

```bash
# Клонировать репозиторий
git clone https://github.com/vicomarcellus/petapp.git
cd petapp

# Установить зависимости
npm install

# Создать .env файл
cp .env.example .env
# Заполнить переменные окружения

# Запустить dev сервер
npm run dev
```

## Переменные окружения

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_OPENAI_API_KEY=your_openai_api_key (опционально)
```

## База данных

SQL миграции находятся в папке `sql/`. Выполни их в Supabase SQL Editor в следующем порядке:

1. `create_notes_table.sql` - основная таблица записей
2. `create_note_tags_table.sql` - теги для симптомов
3. `create_medication_tags_table.sql` - теги для лекарств
4. `create_food_tags_table.sql` - теги для питания
5. `create_diagnoses_table.sql` - диагнозы
6. `create_diagnosis_notes_table.sql` - заметки к диагнозам
7. `create_attachments_table.sql` - вложения
8. `create_storage_bucket.sql` - хранилище файлов
9. `create_units_table.sql` - единицы измерения
10. Остальные `add_*.sql` и `migrate_*.sql` - дополнительные поля

## Деплой

Проект настроен для деплоя на Vercel:

```bash
npm run build
vercel
```

## Лицензия

MIT
